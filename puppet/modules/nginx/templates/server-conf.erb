server {
  listen 80;
  #listen [::]:80 ipv6only=on;

  server_name <%= @name %>.dev;

  location /static {
    alias /srv/www/<%= @name %>/src/static;
  }

  location / {
    try_files $uri @app;
  }

  location @app {
    include uwsgi_params;
    uwsgi_pass unix:///tmp/<%= @socket %>.sock;
  }
}